/************
 Processor: ts
 ************/

import * as dependency_0 from '$[scope]$[name]/provider/ts';

// FILE: index.d.ts
declare namespace ns_index {
    import Provider = dependency_0.Provider;

    class Model {
        provider: Provider;

        get(id: number): {
            id: number;
            name: string;
            lastname: string;
        } | "User not exist";

        list(): any[];
    }
}

export import Model = ns_index.Model;


export declare const hmr: { on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };